name: Sync EasyTier Release

on:
  schedule:
    - cron: "0 3 * * *"   # 每天 03:00 UTC
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Get latest EasyTier release
        id: latest
        run: |
          TAG=$(curl -s https://api.github.com/repos/EasyTier/EasyTier/releases/latest | jq -r .tag_name)
          if [ -z "$TAG" ] || [ "$TAG" = "null" ]; then
            echo "::warning::Got empty tag from GitHub API (rate-limited?), skipping."
            echo "skip=true" >> $GITHUB_OUTPUT
          else
            echo "latest=$TAG" >> $GITHUB_OUTPUT
            echo "skip=false" >> $GITHUB_OUTPUT
          fi

      - name: Read current version
        if: steps.latest.outputs.skip != 'true'
        id: current
        run: |
          CUR=$(cat VERSION)
          echo "current=$CUR" >> $GITHUB_OUTPUT

      - name: Update VERSION if needed
        if: steps.latest.outputs.skip != 'true' && steps.latest.outputs.latest != steps.current.outputs.current
        run: |
          echo "${{ steps.latest.outputs.latest }}" > VERSION
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add VERSION
          git commit -m "chore: bump EasyTier to ${{ steps.latest.outputs.latest }}"
          git push
